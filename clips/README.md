# Video Clips

Drop your `.mp4` short clips here. They will be served at `/clips/your-file.mp4`.

To display a clip in the Gallery, open `src/components/Gallery.jsx` and add an entry:

```js
{
  type: 'video',
  src: '/clips/your-file.mp4',
  alt: 'Description of the clip',
}
```
